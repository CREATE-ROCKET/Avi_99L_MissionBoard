# Characterizationの高精度timer callbackをtask dispatchではなくISRから直接dispatchする。
CONFIG_ESP_TIMER_SUPPORTS_ISR_DISPATCH_METHOD=y

# char_runtimeとchar_encoderはいずれもCPU1固定なので、timer ISRもCPU1へ揃える。
# CONFIG_ESP_TIMER_ISR_AFFINITY_CPU0 is not set
CONFIG_ESP_TIMER_ISR_AFFINITY_CPU1=y
